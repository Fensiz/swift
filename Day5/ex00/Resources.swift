import UIKit

enum Resources {
	enum Strings {
		enum TabBar {
			static var map = "Map"
			static var list = "List"
			static var more = "More"
		}
	}
	enum Images {
		enum TabBar {
			static var map = UIImage(systemName: "map")
			static var list = UIImage(systemName: "list.bullet")
			static var more = UIImage(systemName: "ellipsis")
		}
	}
}
